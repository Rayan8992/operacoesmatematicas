name: C/C++ CI

on:
  push:
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3

    - name: Compile test file
      run: |
        gcc multiplicar.c dividir.c operacoesmatematicas_test.c -o teste

    - name: Run tests
      run: ./teste
